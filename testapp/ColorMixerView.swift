import SwiftUI

struct ColorMixerView: View {
    var body: some View {
        VStack {
            Text("Color Mixer Screen")
                .font(.largeTitle)
        }
        .navigationTitle("Color Mixer")
    }
}

#Preview {
    ColorMixerView()
}
