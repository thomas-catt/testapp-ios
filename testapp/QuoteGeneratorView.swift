import SwiftUI

struct QuoteGeneratorView: View {
    var body: some View {
        VStack {
            Text("Quote Generator Screen")
                .font(.largeTitle)
        }
        .navigationTitle("Quote Generator")
    }
}

#Preview {
    QuoteGeneratorView()
}
